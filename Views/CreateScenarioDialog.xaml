<Window x:Class="MMG.Views.CreateScenarioDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="새 시나리오 만들기" 
        Height="220" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   Text="새 테스트 시나리오 만들기" 
                   FontSize="16" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"
                   HorizontalAlignment="Center"/>
        
        <!-- Name Input -->
        <TextBlock Grid.Row="1" 
                   Text="시나리오 이름:" 
                   Margin="0,0,0,5"/>
        
        <TextBox Grid.Row="2"
                 x:Name="ScenarioNameTextBox"
                 Height="30" 
                 Margin="0,0,0,20"
                 FontSize="14"/>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="취소" 
                    Width="80" 
                    Height="30" 
                    Margin="0,0,10,0"
                    Click="Cancel_Click"/>
            <Button Content="생성" 
                    Width="80" 
                    Height="30" 
                    Click="Create_Click"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>