<UserControl x:Class="MMG.Views.TestsContentPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" 
                    Orientation="Horizontal" 
                    Margin="20,20,20,10">
            <TextBlock Text="Tests" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="#2C3E50"/>
            <Button Content="New Test" 
                    Width="100" 
                    Height="30" 
                    Margin="20,0,0,0"
                    Background="#3498DB"
                    Foreground="White"
                    BorderThickness="0"/>
        </StackPanel>
        
        <!-- Content Area -->
        <Grid Grid.Row="1" Margin="20,10,20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Test List -->
            <Border Grid.Column="0" 
                    Background="#F8F9FA" 
                    BorderBrush="#E9ECEF" 
                    BorderThickness="1">
                <StackPanel Margin="10">
                    <TextBlock Text="Test Collections" 
                               FontWeight="Bold" 
                               Margin="0,0,0,10"/>
                    <ListBox Background="Transparent" 
                             BorderThickness="0">
                        <ListBoxItem Content="Collection 1"/>
                        <ListBoxItem Content="Collection 2"/>
                        <ListBoxItem Content="Collection 3"/>
                    </ListBox>
                </StackPanel>
            </Border>
            
            <!-- Splitter -->
            <GridSplitter Grid.Column="1" 
                          HorizontalAlignment="Stretch" 
                          Width="5" 
                          Background="#EEEEEE"/>
            
            <!-- Test Details -->
            <Border Grid.Column="2" 
                    Background="#F8F9FA" 
                    BorderBrush="#E9ECEF" 
                    BorderThickness="1">
                <StackPanel Margin="10">
                    <TextBlock Text="Test Details" 
                               FontWeight="Bold" 
                               Margin="0,0,0,10"/>
                    
                    <TextBox Text="{Binding TestName}" 
                             Margin="0,0,0,10"
                             Height="30">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Foreground" Value="Black"/>
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <VisualBrush Stretch="None">
                                                    <VisualBrush.Visual>
                                                        <TextBlock Text="Test Name" 
                                                                   FontStyle="Italic" 
                                                                   Foreground="Gray" 
                                                                   VerticalAlignment="Center" 
                                                                   Margin="5,0,0,0"/>
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    
                    <TextBox Text="{Binding TestDescription}"
                             Height="80"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             Margin="0,0,0,10">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Foreground" Value="Black"/>
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <VisualBrush Stretch="None">
                                                    <VisualBrush.Visual>
                                                        <TextBlock Text="Test Description" 
                                                                   FontStyle="Italic" 
                                                                   Foreground="Gray" 
                                                                   VerticalAlignment="Top" 
                                                                   Margin="5,5,0,0"/>
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Run Test" 
                                Width="80" 
                                Height="30" 
                                Background="#2ECC71"
                                Foreground="White"
                                BorderThickness="0"
                                Margin="0,0,10,0"/>
                        <Button Content="Save Test" 
                                Width="80" 
                                Height="30" 
                                Background="#3498DB"
                                Foreground="White"
                                BorderThickness="0"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- Status Bar -->
        <Border Grid.Row="2" 
                Background="#34495E" 
                Height="30">
            <TextBlock Text="Ready" 
                       Foreground="White" 
                       VerticalAlignment="Center" 
                       Margin="10,0"/>
        </Border>
    </Grid>
</UserControl>