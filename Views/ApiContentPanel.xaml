<UserControl x:Class="MMG.Views.ApiContentPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:MMG.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" MinWidth="300"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="3*" MinWidth="400"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="1.5*" MinWidth="300"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel - Saved Requests -->
        <views:SavedRequestsPanel Grid.Column="0"/>

        <!-- Splitter 1 -->
        <GridSplitter Grid.Column="1"
                      HorizontalAlignment="Stretch"
                      Width="10"
                      Background="#EEEEEE"/>

        <!-- Middle Panel - Request -->
        <views:RequestPanel Grid.Column="2"/>

        <!-- Splitter 2 -->
        <GridSplitter Grid.Column="3"
                      HorizontalAlignment="Stretch"
                      Width="10"
                      Background="#EEEEEE"/>

        <!-- Right Panel - Response -->
        <views:ResponsePanel Grid.Column="4"/>
    </Grid>
</UserControl>