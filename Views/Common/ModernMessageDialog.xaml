<Window x:Class="MMG.Views.Common.ModernMessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="알림"
        MinWidth="380"
        MinHeight="200"
        MaxWidth="500"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False">

    <Window.Resources>
        <!-- Icon Colors -->
        <SolidColorBrush x:Key="SuccessIconColor"
                Color="#10B981"/>
        <SolidColorBrush x:Key="ErrorIconColor"
                Color="#EF4444"/>
        <SolidColorBrush x:Key="WarningIconColor"
                Color="#F59E0B"/>
        <SolidColorBrush x:Key="InfoIconColor"
                Color="#3B82F6"/>
    </Window.Resources>

    <Border Background="White"
            CornerRadius="12"
            BorderBrush="#E5E7EB"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20"
                              ShadowDepth="2"
                              Opacity="0.2"
                              Color="#000000"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header with drag support -->
            <Border Grid.Row="0"
                    Background="#F9FAFB"
                    CornerRadius="12,12,0,0"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid Margin="20,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Icon -->
                    <Border Grid.Column="0"
                            x:Name="IconBorder"
                            Width="40"
                            Height="40"
                            CornerRadius="20"
                            Margin="0,0,12,0">
                        <TextBlock x:Name="IconText"
                                   FontSize="20"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>

                    <!-- Title -->
                    <TextBlock Grid.Column="1"
                               x:Name="TitleText"
                               Text="알림"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="#111827"
                               VerticalAlignment="Center"/>

                    <!-- Close Button -->
                    <Button Grid.Column="2"
                            Content="✕"
                            Width="28"
                            Height="28"
                            FontSize="12"
                            Background="Transparent"
                            BorderThickness="0"
                            Foreground="#9CA3AF"
                            Cursor="Hand"
                            Click="Close_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border"
                                                    Background="{TemplateBinding Background}"
                                                    CornerRadius="6">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver"
                                                        Value="True">
                                                    <Setter TargetName="border"
                                                            Property="Background"
                                                            Value="#F3F4F6"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          MaxHeight="300"
                          Margin="20,16">
                <TextBlock x:Name="MessageText"
                           Text="메시지 내용"
                           FontSize="14"
                           Foreground="#4B5563"
                           TextWrapping="Wrap"
                           LineHeight="22"/>
            </ScrollViewer>

            <!-- Buttons -->
            <Border Grid.Row="2"
                    Background="#F9FAFB"
                    CornerRadius="0,0,12,12"
                    Padding="20,16">
                <StackPanel x:Name="ButtonPanel"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <!-- Buttons will be added dynamically -->
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
