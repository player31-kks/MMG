<Window x:Class="MMG.Views.Common.CreateFolderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="새 폴더 만들기"
        Width="360"
        Height="160"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="폴더 이름:"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <TextBox Grid.Row="1"
                 x:Name="FolderNameTextBox"
                 Height="30"
                 Padding="10,5"
                 BorderBrush="#DDDDDD"
                 BorderThickness="1"
                 VerticalContentAlignment="Center"
                 Margin="0,0,0,10"/>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="취소"
                    Click="CancelButton_Click"
                    Width="70"
                    Height="30"
                    Margin="0,0,10,10"
                    Style="{StaticResource CancelButtonStyle}"/>

            <Button Content="만들기"
                    Style="{StaticResource ConfirmationButtonStyle}"
                    Click="CreateButton_Click"
                    Width="70"
                    Height="30"
                    Margin="0,0,0,10"
                    />
        </StackPanel>
    </Grid>
</Window>