<Window x:Class="MMG.Views.Common.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="설정"
        Width="480"
        Height="280"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="포트 설정"
                   FontSize="{DynamicResource text-lg}"
                   FontWeight="SemiBold"
                   Margin="0,0,0,10"/>

        <!-- Use Custom Port -->
        <CheckBox Grid.Row="1"
                  Content="커스텀 포트 사용"
                  IsChecked="{Binding UseCustomPort, Mode=TwoWay}"
                  Margin="0,0,0,10"
                  FontSize="{DynamicResource text-sm}"/>

        <!-- Custom Port Setting -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    Margin="0,0,0,10">
            <TextBlock Text="커스텀 포트:"
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"
                       FontSize="{DynamicResource text-sm}"/>
            <TextBox Text="{Binding CustomPort, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Width="80"
                     Height="30"
                     VerticalContentAlignment="Center"
                     IsEnabled="{Binding UseCustomPort}"/>
        </StackPanel>

        <!-- Description -->
        <TextBlock Grid.Row="3"
                   TextWrapping="Wrap"
                   Margin="0,0,0,5"
                   FontSize="{DynamicResource text-xs}"
                   Foreground="Gray">
            <Run Text="• 커스텀 포트 사용 시: 모든 주소로 전송할 때 지정된 포트를 사용합니다."/>
            <LineBreak/>
            <Run Text="• 비활성화 시: 모든 주소에 대해 대상 포트와 동일한 포트를 사용합니다."/>
            <LineBreak/>
            <Run Text="• 기본값: 8080"/>
        </TextBlock>

        <!-- Buttons -->
        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="확인"
                    Width="80"
                    Height="30"
                    Style="{StaticResource ModernButton}"
                    Click="OkButton_Click"
                    Margin="0,0,10,0"/>
            <Button Content="취소"
                    Width="80"
                    Height="30"
                    Style="{StaticResource CancelButtonStyle}"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>