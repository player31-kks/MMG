<UserControl x:Class="MMG.Views.SavedRequestsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:MMG"
             xmlns:converters="clr-namespace:MMG.Converters"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="300">
    <Border Background="White"
            BorderBrush="#DDDDDD"
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0"
                    Background="#F8F9FA"
                    BorderBrush="#DDDDDD"
                    BorderThickness="0,0,0,1"
                    Padding="15,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="Saved Requests"
                               FontSize="16"
                               FontWeight="Bold"
                               VerticalAlignment="Center"/>

                    <Button Grid.Column="1"
                            Content="↻"
                            Command="{Binding RefreshCommand}"
                            Style="{StaticResource ModernButton}"
                            Width="30"
                            Height="25"
                            FontSize="14"
                            ToolTip="새로고침"/>
                </Grid>
            </Border>

            <!-- Saved Requests List -->
            <ListBox Grid.Row="1"
                     ItemsSource="{Binding SavedRequests}"
                     SelectedItem="{Binding SelectedSavedRequest}"
                     Margin="10"
                     BorderThickness="0"
                     Background="Transparent">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White"
                                BorderBrush="#E0E0E0"
                                BorderThickness="1"
                                CornerRadius="4"
                                Margin="0,2"
                                Padding="10,8"
                                Cursor="Hand">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver"
                                                Value="True">
                                            <Setter Property="Background"
                                                    Value="#F5F5F5"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0"
                                           Text="{Binding Name}"
                                           FontWeight="Bold"
                                           FontSize="13"
                                           Foreground="#333333"
                                           TextTrimming="CharacterEllipsis"/>

                                <TextBlock Grid.Row="1"
                                           FontSize="11"
                                           Foreground="#666666"
                                           Margin="0,2,0,0">
                                    <Run Text="{Binding IpAddress}"/>
                                    <Run Text=":"/>
                                    <Run Text="{Binding Port}"/>
                                </TextBlock>

                                <TextBlock Grid.Row="2"
                                           Text="{Binding LastModified, StringFormat='Modified: {0:MM/dd HH:mm}'}"
                                           FontSize="10"
                                           Foreground="#999999"
                                           Margin="0,2,0,0"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment"
                                Value="Stretch"/>
                        <Setter Property="Padding"
                                Value="0"/>
                        <Setter Property="Margin"
                                Value="0"/>
                        <Setter Property="Background"
                                Value="Transparent"/>
                        <Setter Property="BorderThickness"
                                Value="0"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected"
                                     Value="True">
                                <Setter Property="Background"
                                        Value="#E3F2FD"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

            <!-- Action Buttons -->
            <Border Grid.Row="2"
                    Background="#F8F9FA"
                    BorderBrush="#DDDDDD"
                    BorderThickness="0,1,0,0"
                    Padding="10">
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button Content="New"
                            Command="{Binding NewRequestCommand}"
                            Style="{StaticResource ModernButton}"
                            Width="60"
                            Height="25"
                            FontSize="11"
                            Margin="0,0,5,0"
                            Background="#4CAF50"
                            ToolTip="새로운 요청 만들기"/>

                    <Button Content="Delete"
                            Command="{Binding DeleteSelectedCommand}"
                            Style="{StaticResource ModernButton}"
                            Width="60"
                            Height="25"
                            FontSize="11"
                            Background="#FF5252"
                            IsEnabled="{Binding SelectedSavedRequest, Converter={x:Static converters:NullToBooleanConverter.Instance}}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>