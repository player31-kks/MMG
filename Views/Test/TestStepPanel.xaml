<UserControl x:Class="MMG.Views.Test.TestStepPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:MMG.Models"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="400"
             Background="{StaticResource PanelBackground}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{StaticResource SoftAccent}"
                BorderBrush="{StaticResource MutedBorder}"
                BorderThickness="0"
                CornerRadius="10"
                Padding="14"
                Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="테스트 단계"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="{StaticResource PrimaryColor}"/>
                    <TextBlock Text="{Binding SelectedScenario.Name, StringFormat='시나리오: {0}'}"
                               FontSize="12"
                               Foreground="{StaticResource MutedText}"
                               Margin="0,2,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility"
                                        Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedScenario}"
                                                 Value="{x:Null}">
                                        <Setter Property="Visibility"
                                                Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SelectedScenario}"
                                                 Value="{x:Null}">
                                        <Setter Property="Visibility"
                                                Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal">
                    <Button Content="📋"
                            Click="OpenLogWindow_Click"
                            Width="30"
                            Height="30"
                            FontSize="13"
                            Style="{StaticResource ModernButton}"
                            Background="#9CA3AF"
                            ToolTip="실시간 로그 창 열기"
                            Margin="0,0,6,0"/>
                    <Button Content="+"
                            Command="{Binding AddTestStepCommand}"
                            Width="30"
                            Height="30"
                            FontSize="18"
                            FontWeight="Light"
                            Style="{StaticResource ModernButton}"
                            ToolTip="새 테스트 단계 추가"
                            Margin="0,0,6,0"/>

                    <!-- 실행 버튼 (미실행 상태일 때) -->
                    <Button Command="{Binding RunSelectedScenarioCommand}"
                            Width="30"
                            Height="30"
                            ToolTip="시나리오 실행">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource ModernButton}">
                                <Setter Property="Background" Value="{StaticResource SecondaryColor}"/>
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsTestRunning}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <TextBlock Text="▶" FontSize="12" VerticalAlignment="Center"/>
                    </Button>

                    <!-- 중지 버튼 (실행 중일 때) -->
                    <Button Command="{Binding StopTestCommand}"
                            Width="30"
                            Height="30"
                            ToolTip="테스트 중지">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource ModernButton}">
                                <Setter Property="Background" Value="{StaticResource WarningColor}"/>
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsTestRunning}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock FontSize="10" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="■"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsStopping}" Value="True">
                                                <Setter Property="Text" Value="…"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Test Steps List -->
        <ListBox x:Name="StepsListBox"
                 Grid.Row="1"
                 ItemsSource="{Binding SelectedScenario.TestSteps}"
                 SelectedItem="{Binding SelectedStep}"
                 Background="Transparent"
                 BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 Padding="8,8,8,0"
                 AllowDrop="True"
                 PreviewMouseLeftButtonDown="StepsListBox_PreviewMouseLeftButtonDown"
                 PreviewMouseMove="StepsListBox_PreviewMouseMove"
                 Drop="StepsListBox_Drop"
                 DragOver="StepsListBox_DragOver">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type models:TestStep}">
                    <Border x:Name="StepBorder"
                            Style="{StaticResource ClickableTestStepBorder}"
                            AllowDrop="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- 드래그 핸들 -->
                            <Border Grid.Column="0"
                                    Width="20"
                                    Background="Transparent"
                                    Cursor="SizeAll"
                                    ToolTip="드래그하여 순서 변경"
                                    Margin="0,0,4,0">
                                <TextBlock Text="⋮⋮"
                                           FontSize="12"
                                           Foreground="#ADB5BD"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"/>
                            </Border>
                            
                            <!-- 스텝 내용 -->
                            <Grid Grid.Column="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Step Header -->
                                <Grid Grid.Row="0"
                                      Margin="0,0,0,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- 백그라운드 스텝 여부에 따라 다른 뱃지 색상 -->
                                    <Border>
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#6F5FF5"/>
                                                <Setter Property="CornerRadius" Value="10"/>
                                                <Setter Property="Width" Value="22"/>
                                                <Setter Property="Height" Value="22"/>
                                                <Setter Property="Margin" Value="0,0,8,0"/>
                                                <Setter Property="ToolTip" Value="순차 실행"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsBackground}" Value="True">
                                                        <Setter Property="Background" Value="#FF8C00"/>
                                                        <Setter Property="ToolTip" Value="백그라운드 실행"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding StepNumber}"
                                                   Foreground="White"
                                                   FontSize="11"
                                                   FontWeight="Bold"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"/>
                                    </Border>

                                    <StackPanel Grid.Column="1"
                                                VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Name}"
                                                       FontSize="14"
                                                       FontWeight="SemiBold"
                                                       Foreground="{StaticResource TextPrimaryColor}"/>
                                            <!-- 백그라운드 표시 아이콘 -->
                                            <TextBlock Text=" ⚡"
                                                       FontSize="12"
                                                       Foreground="#FF8C00"
                                                       VerticalAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsBackground}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                        <TextBlock Text="{Binding StepTypeDisplay}"
                                                   FontSize="11"
                                                   Foreground="{StaticResource MutedText}"
                                                   Margin="0,2,0,0"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2"
                                                VerticalAlignment="Center">
                                        <TextBlock Text="{Binding StatusText}"
                                                   FontSize="10"
                                                   Foreground="{StaticResource PrimaryColor}"
                                                   HorizontalAlignment="Right">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility"
                                                            Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding StatusText}"
                                                                     Value="">
                                                            <Setter Property="Visibility"
                                                                    Value="Collapsed"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding StatusText}"
                                                                     Value="{x:Null}">
                                                            <Setter Property="Visibility"
                                                                    Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </Grid>

                                <!-- Last Result -->
                                <Border Grid.Row="2"
                                        Background="#F8F9FA"
                                        BorderBrush="#E9ECEF"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Padding="8"
                                        Margin="0,0,0,4">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility"
                                                    Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding LastResult}"
                                                             Value="{x:Null}">
                                                    <Setter Property="Visibility"
                                                            Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <StackPanel Grid.Row="0"
                                                    Orientation="Horizontal"
                                                    Margin="0,0,0,4">
                                            <TextBlock Text="결과:"
                                                       FontSize="11"
                                                       FontWeight="SemiBold"
                                                       Foreground="#495057"
                                                       Margin="0,0,8,0"/>
                                            <TextBlock FontSize="11"
                                                       FontWeight="SemiBold">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Text"
                                                                Value="성공"/>
                                                        <Setter Property="Foreground"
                                                                Value="#22C55E"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding LastResult.IsSuccess}"
                                                                         Value="False">
                                                                <Setter Property="Text"
                                                                        Value="실패"/>
                                                                <Setter Property="Foreground"
                                                                        Value="{StaticResource WarningColor}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <TextBlock Text="{Binding LastResult.ExecutionTime, StringFormat=F2}"
                                                       FontSize="11"
                                                       Foreground="#6C757D"
                                                       Margin="8,0,0,0"/>
                                        </StackPanel>

                                        <TextBlock Grid.Row="1"
                                                   Text="{Binding LastResult.Message}"
                                                   FontSize="11"
                                                   Foreground="#6C757D"
                                                   TextWrapping="Wrap"
                                                   MaxHeight="60"/>
                                    </Grid>
                                </Border>

                                <!-- Progress indicator for running steps -->
                                <ProgressBar Grid.Row="3"
                                             IsIndeterminate="True"
                                             Height="2"
                                             Background="Transparent"
                                             Foreground="{StaticResource PrimaryColor}"
                                             BorderThickness="0">
                                    <ProgressBar.Style>
                                        <Style TargetType="ProgressBar">
                                            <Setter Property="Visibility"
                                                    Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsRunning}"
                                                             Value="True">
                                                    <Setter Property="Visibility"
                                                            Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ProgressBar.Style>
                                </ProgressBar>
                            </Grid>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Empty State -->
        <StackPanel Grid.Row="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility"
                            Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedScenario.TestSteps.Count}"
                                     Value="0">
                            <Setter Property="Visibility"
                                    Value="Visible"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding SelectedScenario}"
                                     Value="{x:Null}">
                            <Setter Property="Visibility"
                                    Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <TextBlock Text="📋"
                       FontSize="48"
                       HorizontalAlignment="Center"
                       Opacity="0.3"
                       Margin="0,0,0,16"/>
            <TextBlock Text="테스트 단계가 없습니다"
                       FontSize="16"
                       Foreground="{StaticResource MutedText}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,8"/>
            <TextBlock Text="+ 버튼을 클릭하여 새 단계를 추가하세요"
                       FontSize="12"
                       Foreground="{StaticResource MutedText}"
                       Opacity="0.6"
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Footer -->
        <Border Grid.Row="2"
                Style="{StaticResource PanelFooter}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0"
                            Orientation="Horizontal">
                    <TextBlock Text="총"
                               FontSize="12"
                               Foreground="#6C757D"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding SelectedScenario.TestSteps.Count}"
                               FontSize="12"
                               FontWeight="Bold"
                               Foreground="#495057"
                               Margin="4,0"/>
                    <TextBlock Text="개 단계"
                               FontSize="12"
                               Foreground="#6C757D"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>