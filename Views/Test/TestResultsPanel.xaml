<UserControl x:Class="MMG.Views.Test.TestResultsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:MMG.Models"
             mc:Ignorable="d"
             d:DesignHeight="400"
             d:DesignWidth="350"
             Background="{StaticResource SurfaceBackground}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Style="{StaticResource PanelHeader}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="실행 결과"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="{StaticResource PrimaryColor}"/>
                    <TextBlock Text="테스트 실행 결과 및 통계"
                               FontSize="12"
                               Foreground="{StaticResource MutedText}"
                               Margin="0,2,0,0"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="📊"
                        Command="{Binding OpenDetailedResultsCommand}"
                        Width="30"
                        Height="30"
                        FontSize="14"
                        Style="{StaticResource ModernButton}"
                        ToolTip="상세 결과 보기"/>
            </Grid>
        </Border>

        <!-- Results Summary -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      Padding="20">
            <StackPanel>

                <!-- Overall Statistics -->
                <Border Background="White"
                        BorderBrush="#DEE2E6"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="16"
                        Margin="0,0,0,16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Text="전체 통계"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryColor}"
                                   Margin="0,0,0,12"/>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0"
                                    Grid.Column="0"
                                       Text="총 시나리오:"
                                       FontSize="12"
                                       Foreground="{StaticResource MutedText}"/>
                            <TextBlock Grid.Row="0"
                                    Grid.Column="1"
                                       Text="{Binding TotalScenarios}"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryColor}"
                                       HorizontalAlignment="Right"/>

                            <TextBlock Grid.Row="1"
                                    Grid.Column="0"
                                       Text="성공:"
                                       FontSize="12"
                                       Foreground="{StaticResource MutedText}"/>
                            <TextBlock Grid.Row="1"
                                    Grid.Column="1"
                                       Text="{Binding SuccessfulTests}"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="#22C55E"
                                       HorizontalAlignment="Right"/>

                            <TextBlock Grid.Row="2"
                                    Grid.Column="0"
                                       Text="실패:"
                                       FontSize="12"
                                       Foreground="{StaticResource MutedText}"/>
                            <TextBlock Grid.Row="2"
                                    Grid.Column="1"
                                       Text="{Binding FailedTests}"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource WarningColor}"
                                       HorizontalAlignment="Right"/>

                            <TextBlock Grid.Row="3"
                                    Grid.Column="0"
                                       Text="성공률:"
                                       FontSize="12"
                                       Foreground="{StaticResource MutedText}"/>
                            <TextBlock Grid.Row="3"
                                    Grid.Column="1"
                                       Text="{Binding SuccessRate, StringFormat=N1}"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Right"
                                       Foreground="#22C55E"/>
                        </Grid>
                    </Grid>
                </Border>

                <!-- Recent Results -->
                <Border Background="White"
                        BorderBrush="#DEE2E6"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Text="최근 결과"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryColor}"
                                   Margin="0,0,0,12"/>

                        <ItemsControl Grid.Row="1"
                                      ItemsSource="{Binding RecentResults}"
                                      MaxHeight="200">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type models:TestResult}">
                                    <Border Background="#F8F9FA"
                                            BorderBrush="#E9ECEF"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            Padding="10"
                                            Margin="0,0,0,6">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <Ellipse Grid.Column="0"
                                                     Width="8"
                                                     Height="8"
                                                     VerticalAlignment="Center"
                                                     Margin="0,0,8,0">
                                                <Ellipse.Style>
                                                    <Style TargetType="Ellipse">
                                                        <Setter Property="Fill"
                                                                Value="#22C55E"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsSuccess}"
                                                                    Value="False">
                                                                <Setter Property="Fill"
                                                                        Value="{StaticResource WarningColor}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Ellipse.Style>
                                            </Ellipse>

                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding ScenarioName}"
                                                           FontSize="11"
                                                           FontWeight="SemiBold"
                                                           Foreground="{StaticResource TextPrimaryColor}"/>
                                                <TextBlock Text="{Binding Message}"
                                                           FontSize="10"
                                                           Foreground="{StaticResource MutedText}"
                                                           TextWrapping="Wrap"
                                                           Margin="0,2,0,0"/>
                                            </StackPanel>

                                            <TextBlock Grid.Column="2"
                                                       Text="{Binding ExecutionTime, StringFormat=F0}"
                                                       FontSize="10"
                                                       Foreground="{StaticResource MutedText}"
                                                       VerticalAlignment="Top"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2"
                Style="{StaticResource PanelFooter}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding LastUpdateTime, StringFormat=T}"
                           FontSize="11"
                           Foreground="{StaticResource MutedText}"
                           VerticalAlignment="Center"/>

                <Button Grid.Column="1"
                        Content="새로고침"
                        Command="{Binding RefreshResultsCommand}"
                        Style="{StaticResource ModernButton}"
                        Padding="12,6"
                        FontSize="11"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>