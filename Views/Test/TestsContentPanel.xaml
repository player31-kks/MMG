<UserControl x:Class="MMG.Views.Test.TestsContentPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:test="clr-namespace:MMG.Views.Test"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="1200"
             Background="{StaticResource SurfaceBackground}">

    <Grid Background="{StaticResource SurfaceBackground}">
        <Border Background="{StaticResource PanelBackground}"
                BorderBrush="{StaticResource MutedBorder}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="14"
                Margin="14"
                SnapsToDevicePixels="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="360"
                                      MinWidth="300"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"
                                      MinWidth="420"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="350"
                                      MinWidth="300"/>
                </Grid.ColumnDefinitions>

                <!-- Left Panel - Saved Scenarios -->
                <Border Grid.Column="0"
                        Background="{StaticResource PanelBackground}"
                        BorderBrush="{StaticResource MutedBorder}"
                        BorderThickness="0,0,1,0"
                        CornerRadius="10"
                        Padding="4">
                    <test:SavedScenariosPanel/>
                </Border>

                <!-- Splitter 1 -->
                <GridSplitter Grid.Column="1"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Stretch"
                              Width="10"
                              Background="Transparent"
                              ShowsPreview="False"
                              Cursor="SizeWE">
                    <GridSplitter.Template>
                        <ControlTemplate TargetType="GridSplitter">
                            <Border Background="Transparent">
                                <Rectangle Width="2"
                                           Fill="{StaticResource MutedBorder}"
                                           HorizontalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </GridSplitter.Template>
                </GridSplitter>

                <!-- Middle Panel - Test Steps -->
                <Border Grid.Column="2"
                        Background="{StaticResource PanelBackground}"
                        Padding="10,0">
                    <test:TestStepPanel/>
                </Border>

                <!-- Splitter 2 -->
                <GridSplitter Grid.Column="3"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Stretch"
                              Width="10"
                              Background="Transparent"
                              ShowsPreview="False"
                              Cursor="SizeWE">
                    <GridSplitter.Template>
                        <ControlTemplate TargetType="GridSplitter">
                            <Border Background="Transparent">
                                <Rectangle Width="2"
                                           Fill="{StaticResource MutedBorder}"
                                           HorizontalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </GridSplitter.Template>
                </GridSplitter>

                <!-- Right Panel - Step Configuration -->
                <Border Grid.Column="4"
                        Background="{StaticResource PanelBackground}"
                        BorderBrush="{StaticResource MutedBorder}"
                        BorderThickness="1,0,0,0"
                        CornerRadius="10"
                        Padding="10,0,6,0">
                    <test:StepConfigurationPanel/>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>