<UserControl x:Class="MMG.Views.RequestPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MMG"
             xmlns:models="clr-namespace:MMG.Models"
             xmlns:views="clr-namespace:MMG.Views"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="400">
    <Border Background="White"
            BorderBrush="#DDDDDD"
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Connection Info -->
            <Border Grid.Row="0"
                    Background="#F8F9FA"
                    BorderBrush="#DDDDDD"
                    BorderThickness="0,0,0,1"
                    Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0"
                               Grid.Column="0"
                               Text="UDP Request"
                               FontSize="16"
                               FontWeight="Bold"
                               Margin="0,0,0,10"/>

                    <Label Grid.Row="1"
                           Grid.Column="0"
                           Content="IP:"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                    <TextBox Grid.Row="1"
                             Grid.Column="1"
                             Text="{Binding CurrentRequest.IpAddress}"
                             Style="{StaticResource ModernTextBox}"
                             Margin="0,0,10,0"/>
                    <Label Grid.Row="1"
                           Grid.Column="2"
                           Content="Port:"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                    <TextBox Grid.Row="1"
                             Grid.Column="3"
                             Text="{Binding CurrentRequest.Port}"
                             Style="{StaticResource ModernTextBox}"/>
                </Grid>
            </Border>

            <!-- Request Content -->
            <TabControl Grid.Row="1"
                        Margin="20"
                        Style="{x:Null}">
                <!-- Request Schema Tab -->
                <TabItem Header="Request Schema"
                         Style="{StaticResource ModernTabItem}">
                    <views:RequestSchemaTabs Margin="10"/>
                </TabItem>

                <!-- Response Schema Tab -->
                <TabItem Header="Response Schema"
                         Style="{StaticResource ModernTabItem}">
                    <views:ResponseSchemaTabs Margin="10"/>
                </TabItem>
            </TabControl>

            <!-- Send Button -->
            <Border Grid.Row="2"
                    Background="#F8F9FA"
                    BorderBrush="#DDDDDD"
                    BorderThickness="0,1,0,0"
                    Padding="20">
                <Button Content="Send Request"
                        Command="{Binding SendCommand}"
                        Style="{StaticResource ModernButton}"
                        FontSize="14"
                        FontWeight="Bold"
                        Padding="20,10"
                        HorizontalAlignment="Center"
                        IsEnabled="{Binding IsSending, Converter={x:Static local:BooleanToInverseBooleanConverter.Instance}}"/>
            </Border>
        </Grid>
    </Border>
</UserControl>
